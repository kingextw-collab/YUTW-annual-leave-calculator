name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  # 允許手動從 Actions 頁面運行此工作流程
  workflow_dispatch:

# 配置權限
permissions:
  contents: read
  pages: write # 允許寫入 Pages 內容
  id-token: write # 允許部署

jobs:
  # 1. 構建 Job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js 🔧
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies 📦
        run: npm install

      - name: Build project 🏗️
        run: npm run build

      - name: Setup Pages 🛠️
        uses: actions/configure-pages@v5

      - name: Upload artifact ⬆️
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' # 上傳 dist 資料夾內容

  # 2. 部署 Job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build # 確保在構建完成後才執行
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
